# 设计原则总结

## 1. 开闭原则 (Open-Closed Principle)
- **定义**：软件实体（类、模块、函数等）应该对扩展开放，对修改关闭
- **示例**：通过接口和抽象类实现，允许添加新的子类而不修改现有代码
- **代码体现**：
  - 接口：Daily (定义eat()和classing()方法)
  - 抽象类：School (实现Daily接口)
  - 具体类：Stu, HighStu (通过继承实现扩展)

## 2. 单一职责原则 (Single Responsibility Principle)
- **定义**：一个类应该只有一个引起变化的原因
- **核心思想**：每个类只负责一个功能领域
- **代码体现**：每个类专注于单一职责

## 3. 里氏替换原则 (Liskov Substitution Principle)
- **定义**：子类型必须能够替换掉它们的父类型
- **核心思想**：子类可以扩展父类的功能，但不能改变父类原有的功能

## 4. 接口隔离原则 (Interface Segregation Principle)
- **定义**：客户端不应该依赖它不需要的接口
- **核心思想**：建立单一接口，不要建立大的的接口
- **代码体现**：接口应该小而专一

## 5. 依赖倒置原则 (Dependency Inversion Principle)
- **定义**：高层模块不应该依赖低层模块，二者都应该依赖抽象
- **核心思想**：抽象不应该依赖细节，细节应该依赖抽象
- **代码体现**：通过接口和抽象类实现依赖关系

## 6. 迪米特法则 (Law of Demeter)
- **定义**：一个对象应该对其他对象有最少的了解
- **核心思想**：只与直接的朋友通信
- **代码体现**：减少类之间的耦合度

## 7. 合成复用原则 (Composite Reuse Principle)
- **定义**：尽量使用对象组合/聚合，而不是继承来达到复用的目的
- **核心思想**：优先使用组合关系，其次才是继承关系
- **代码体现**：通过组合对象来实现功能复用